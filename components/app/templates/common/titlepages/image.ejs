<% if (typeof url !== 'undefined' && url) { %>
<img class="title-page__image" src="<%= typeof url !== 'undefined' ? url: '' %>" unselectable="on">
<% } else {%>
<div class="title-page__image-placeholder" unselectable="on">
    Add image by clicking here!
<div>
<% } %>
