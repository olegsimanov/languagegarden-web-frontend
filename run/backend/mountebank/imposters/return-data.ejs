{
    "port": 3000,
    "protocol": "http",
    "stubs":
    [{
        "predicates": [{
            "contains": {
                "path": "/api/v2/lessons/"
            }
        }],
        "responses": [{
            "inject": "function (request) { let data; if (request.body) data = JSON.parse(request.body); else data = {}; return { body: data, headers: { \"Content-Type\": \"application/json\", \"Access-Control-Allow-Headers\": \"*\", \"Access-Control-Allow-Origin\": \"*\" } }; }"
        }]
    }]
}



